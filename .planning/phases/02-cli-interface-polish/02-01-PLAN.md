---
phase: "02-cli-interface-polish"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified:
  - "internal/task/store.go"
autonomous: true
requirements:
  - "ERR-01"
  - "ERR-02"
  - "ERR-03"
must_haves:
  truths:
    - "Invalid task ID returns 'task not found' with wrapped error context"
    - "Corrupted JSON file returns 'corrupted todo file' error without crashing"
    - "All store errors use fmt.Errorf with %w for error wrapping"
  artifacts:
    - path: "internal/task/store.go"
      provides: "Error-wrapped MarkDone/Delete/loadLocked methods"
      contains: "fmt.Errorf.*%w"
      changes: "lines 138, 156, 54"
key_links:
  - from: "internal/task/store.go"
    to: "ErrTaskNotFound"
    via: "%w wrapping in MarkDone and Delete"
    pattern: "fmt.Errorf.*%w.*ErrTaskNotFound"
  - from: "internal/task/store.go"
    to: "json.Unmarshal"
    via: "error wrapping in loadLocked"
    pattern: "fmt.Errorf.*corrupted todo file.*%w"
---

<objective>
Wire error wrapping with %w in the existing JSONFileStore. This enables CLI to check specific error types with errors.Is().
</objective>

<context>
@internal/task/store.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ErrTaskNotFound with %w in MarkDone and Delete</name>
  <read_first>internal/task/store.go</read_first>
  <acceptance_criteria>
    - grep "store.go" shows "fmt.Errorf.*ErrTaskNotFound" at both call sites (line ~138 and ~156)
    - grep "store.go" shows "%w" verb in both error wrapping calls
    - go vet ./internal/task/... passes with no warnings
  </acceptance_criteria>
  <action>
    Modify store.go line 138 (MarkDone) from:
      return fmt.Errorf("task not found: %s", id)
    To:
      return fmt.Errorf("task not found: %s: %w", id, ErrTaskNotFound)

    Modify store.go line 156 (Delete) from:
      return fmt.Errorf("task not found: %s", id)
    To:
      return fmt.Errorf("task not found: %s: %w", id, ErrTaskNotFound)
  </action>
  <verify>
    <automated>grep -n "ErrTaskNotFound" internal/task/store.go | grep "fmt.Errorf"</automated>
    <manual>go vet ./internal/task/...</manual>
  </verify>
  <done>MarkDone and Delete return wrapped errors using %w pattern with ErrTaskNotFound</done>
</task>

<task type="auto">
  <name>Task 2: Wrap JSON parse error with context in loadLocked</name>
  <read_first>internal/task/store.go</read_first>
  <acceptance_criteria>
    - grep "store.go" shows "corrupted todo file.*%w" wrapping json.Unmarshal error
    - go vet ./internal/task/... passes with no warnings
  </acceptance_criteria>
  <action>
    Modify store.go loadLocked method (line 53-54) to wrap the JSON parse error:
    Change:
      if err := json.Unmarshal(data, &tasks); err != nil {
        return nil, err
      }
    To:
      if err := json.Unmarshal(data, &tasks); err != nil {
        return nil, fmt.Errorf("corrupted todo file: %w", err)
      }
  </action>
  <verify>
    <automated>grep -n "corrupted todo file" internal/task/store.go</automated>
    <manual>go vet ./internal/task/...</manual>
  </verify>
  <done>JSON parse errors wrapped with context using fmt.Errorf("corrupted todo file: %w", err)</done>
</task>

</tasks>

<verification>
- go vet ./internal/task/... passes
- go test ./internal/task/... passes (no regressions from error wrapping changes)
- grep confirms both %w wrapping patterns present
</verification>

<success_criteria>
- [x] MarkDone returns fmt.Errorf("task not found: %s: %w", id, ErrTaskNotFound)
- [x] Delete returns fmt.Errorf("task not found: %s: %w", id, ErrTaskNotFound)
- [x] loadLocked returns fmt.Errorf("corrupted todo file: %w", err) on JSON parse error
- [x] go vet passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-interface-polish/02-01-SUMMARY.md`
</output>
